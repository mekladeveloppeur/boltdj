{
  "name": "boltdj-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd backend && npm install",
    "dev": "cd backend && npm run dev",
    "setup:db": "cd backend && node src/config/seed.js",
    "start": "cd backend && node src/index.js"
  }
}
